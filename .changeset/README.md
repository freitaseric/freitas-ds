# Changesets

Este diretório guarda changesets usados para versionar os pacotes publicáveis do Freitas DS.

Use `pnpm changeset` para registrar uma mudança de release. Use `pnpm version-packages` para aplicar as versões geradas. A publicação real fica em `pnpm release`, mas não deve ser executada sem revisão explícita.
